# 서울 출장마사지 — 간다GO

서울 전지역 방문형 마사지·홈타이 **예약 전 확인 중심** 정적 사이트입니다.
지역 페이지를 많이 찍어내는 대신, 사용자가 예약 전에 실제로 확인해야 하는
정보(방문 가능 주소·가까운 역·건물 출입 방식·예약 가능 시간·추가 이동비)를
중심으로 구성합니다.

## 구조

```
build.py            # 정적 사이트 빌드 스크립트 (content/ → 루트에 HTML 출력)
content/
  site.py           # 공통 설정 (브랜드·전화·텔레그램·NAV)
  pages.py          # 메인 + 예약 전 확인(check) + 이용 장소별(use)
  geo.py            # 생활권(life) · 행정구(district) · 지하철역(station)
  info.py           # 예약 안내 · 고객센터 · 개인정보 · 이용 기준
assets/             # style.css, nav.js, 파비콘 등
```

## 빌드

```bash
python3 build.py
```

- 본문 2,000자 미만 페이지는 자동으로 `noindex` 처리되고 sitemap에서 제외됩니다
  (콘텐츠 품질이 확보된 페이지부터 단계적으로 색인하는 정책).
- 모든 페이지 하단에 공통 요금표가 자동 삽입됩니다.
- Organization / WebPage / Service / BreadcrumbList / FAQPage 스키마가 자동 생성됩니다.

## 배포

Cloudflare Pages가 저장소 루트를 그대로 배포합니다(빌드 명령 없음,
출력 디렉터리 비움). `build.py`는 결과 HTML을 저장소 루트에 직접 출력합니다.

## 상호 / 연락처

- **상호**: 간다GO
- **예약전화**: 0508-202-4719 (연중무휴 24시간)
- **서비스 지역**: 서울특별시 전지역

## 운영 원칙

건전한 방문 관리 서비스만 안내하며, 불법·선정적 서비스, 허위 후기, 과장된
할인 문구는 사용하지 않습니다. 개인정보는 예약에 필요한 최소한만 받고 방문
종료 후 보관하지 않는 것을 원칙으로 합니다.
